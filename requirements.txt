# Film Discovery and Parsing Tool Dependencies
# 
# This file contains all dependencies needed to run:
# - run_parser.py (main entry point)
# - film_discovery_and_parse.py (browser automation + parsing)
# - ultra_reliable_parser.py (HTML parsing engine)

# Core HTTP and async libraries
requests>=2.25.1
aiohttp>=3.8.0

# HTML parsing libraries  
beautifulsoup4>=4.9.3
lxml>=4.6.3

# Browser automation
playwright>=1.40.0

# Data manipulation and analysis
pandas>=1.3.0
numpy>=1.21.0
pytz>=2021.3
python-dateutil>=2.8.2
six>=1.16.0

# Note: Standard library modules (asyncio, time, datetime, etc.) are built-in
# and don't need to be installed separately.
